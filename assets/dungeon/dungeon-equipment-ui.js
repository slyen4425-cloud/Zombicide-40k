/* GenSrpG Dungeon — equipment UI bridge 2.0 / V16.79.01
   UI-only adapter: set progress + rpgBonuses settings.
   RPG characteristic fields are read dynamically from GensRpgStatService167901.
   No movement, combat, timeline or spawn changes. */
(function(){
"use strict";
const ROOT=typeof window!=="undefined"?window:globalThis;
const DOC=typeof document!=="undefined"?document:null;
const VERSION="2.0.0";
const BASE_FIELDS=[
 {key:"armor",label:"Armure",system:true},{key:"defense",label:"Défense",system:true},
 {key:"magicDefense",label:"Défense magique",system:true},{key:"crit",label:"Critique %",system:true},
 {key:"mana",label:"Mana",system:true},{key:"dodge",label:"Esquive %",system:true}
];
let refreshing=false,scheduled=false,observer=null,hookedRenderer=null;
const esc=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const numeric=(value,fallback=0)=>Number.isFinite(Number(value))?Number(value):fallback;
function statDefinitions(){try{const a=ROOT.GensRpgStatService167901?.definitions?.();return Array.isArray(a)?a:[]}catch(e){return []}}
function fields(){
 const out=[],seen=new Set();
 const add=(key,label,system=false)=>{key=String(key||"");if(!key||seen.has(key))return;seen.add(key);out.push({key,label:String(label||key),system})};
 BASE_FIELDS.forEach(x=>add(x.key,x.label,true));
 const defs=statDefinitions();
 if(defs.length)defs.forEach(d=>add(d.id,(d.icon?d.icon+' ':'')+(d.name||d.id),false));
 else [
  ['force','Force'],['agilite','Agilité'],['endurance','Endurance'],['intelligence','Intelligence'],['esprit','Esprit'],['initiative','Initiative']
 ].forEach(([k,l])=>add(k,l,false));
 return out;
}
function thresholds(set){if(Array.isArray(set?.thresholds))return [...set.thresholds].sort((a,b)=>numeric(a?.pieces)-numeric(b?.pieces));return Object.entries(set?.thresholds||{}).map(([pieces,bonuses])=>({pieces:numeric(pieces),bonuses})).sort((a,b)=>a.pieces-b.pieces)}
function bonusText(bonuses){const labels=Object.fromEntries(fields().map(f=>[f.key,f.label]));return Object.entries(bonuses||{}).filter(([,v])=>numeric(v)!==0).map(([k,v])=>{const n=numeric(v);return (n>0?'+':'')+n+' '+(labels[k]||k)}).join(' · ')||'Bonus descriptif'}
function allItems(){try{const a=ROOT.dungeonItems?.();if(Array.isArray(a))return a}catch(e){}try{if(Array.isArray(ROOT.ITEMS))return ROOT.ITEMS}catch(e){}return []}
function findItem(id){id=String(id||'');try{const x=ROOT.itemById?.(id);if(x)return x}catch(e){}return allItems().find(x=>String(x?.id||'')===id)||null}
function equippedItems(){try{const a=ROOT.dungeonEquippedItems?.();return Array.isArray(a)?a.filter(Boolean):[]}catch(e){return []}}
function fallbackSetStates(items){const reg=ROOT.DUNGEON_EQUIPMENT_SETS||{},groups={};for(const item of items||[]){const setId=String(item?.setId||''),set=reg[setId];if(!set)continue;const piece=String(item?.setPieceId||item?.id||'');if(!piece)continue;const g=groups[setId]||(groups[setId]={setId,set,count:0,pieces:new Set(),items:[],activeThresholds:[],bonuses:{}});if(g.pieces.has(piece))continue;g.pieces.add(piece);g.items.push(item);g.count=g.pieces.size}return Object.values(groups)}
function setStates(items=equippedItems()){try{const a=ROOT.dungeonSetStateFromItems316?.(items);if(Array.isArray(a))return a}catch(e){}return fallbackSetStates(items)}
function getSetProgress(items=equippedItems()){return setStates(items).map(state=>{const set=state?.set||ROOT.DUNGEON_EQUIPMENT_SETS?.[state?.setId]||{},count=Math.max(0,numeric(state?.count)),list=thresholds(set),total=Math.max(count,numeric(set?.pieceCount),...list.map(t=>numeric(t?.pieces)));return {setId:String(state?.setId||set?.id||''),name:String(set?.name||state?.setId||'Set'),count,total,items:Array.isArray(state?.items)?state.items:[],earned:list.filter(t=>count>=numeric(t?.pieces)),next:list.filter(t=>count<numeric(t?.pieces))}})}
function thresholdRows(list,active){if(!list.length)return '<div class="deuiEmpty">Aucun</div>';return list.map(t=>'<div class="deuiThreshold '+(active?'active':'next')+'">'+(active?'✅':'🔒')+' '+numeric(t?.pieces)+' pièces : '+esc(bonusText(t?.bonuses))+'</div>').join('')}
function renderSetProgressHtml(progress=getSetProgress()){if(!progress.length)return '';return '<section class="deuiBlock deuiSets"><div class="deuiTitle">🧩 BONUS DE SET</div>'+progress.map(set=>'<article class="deuiSet" data-deui-set="'+esc(set.setId)+'"><div class="deuiSetName">'+esc(set.name)+' — '+set.count+'/'+set.total+'</div>'+(set.items.length?'<div class="deuiPieces">'+set.items.map(i=>esc(i?.name||i?.id||'Pièce')).join(' · ')+'</div>':'')+'<div class="deuiThresholdGroup"><strong>Paliers obtenus</strong>'+thresholdRows(set.earned,true)+'</div><div class="deuiThresholdGroup"><strong>Prochains paliers</strong>'+thresholdRows(set.next,false)+'</div></article>').join('')+'</section>'}
function settingsForItem(item){const bonuses=item?.rpgBonuses&&typeof item.rpgBonuses==='object'?item.rpgBonuses:{};return fields().map(f=>({...f,value:numeric(bonuses[f.key])}))}
function renderItemSettingsHtml(items=equippedItems()){const gear=(items||[]).filter(i=>i&&i.id);if(!gear.length)return '';return '<section class="deuiBlock deuiSettings"><details><summary>⚙️ Réglages équipements</summary><div class="deuiHint">Tous les bonus de caractéristiques utilisent le même registre RPG. Une nouvelle stat apparaît ici automatiquement.</div>'+gear.map(item=>'<article class="deuiItemSettings"><div class="deuiItemName">'+esc(item.name||item.id)+'</div><div class="deuiGrid">'+settingsForItem(item).map(f=>'<label><span>'+esc(f.label)+'</span><input type="number" step="1" value="'+f.value+'" data-deui-item="'+esc(item.id)+'" data-deui-stat="'+esc(f.key)+'"></label>').join('')+'</div></article>').join('')+'</details></section>'}
function findExistingWriter(){for(const name of ['saveDungeonItemOverrides','storeDungeonItemOverrides','persistDungeonItemOverrides'])if(typeof ROOT[name]==='function')return {kind:'bulk',fn:ROOT[name]};for(const name of ['setDungeonItemOverride','updateDungeonItemOverride'])if(typeof ROOT[name]==='function')return {kind:'single',fn:ROOT[name]};return null}
function setEquipmentBonus(itemId,key,rawValue){key=String(key||'');if(!fields().some(f=>f.key===key))return {ok:false,persisted:false};const value=Number(rawValue),item=findItem(itemId);if(!item||!Number.isFinite(value))return {ok:false,persisted:false};item.rpgBonuses={...(item.rpgBonuses||{}),[key]:value};let overrides=null,nextOverride=null,persisted=false;try{if(typeof ROOT.loadDungeonItemOverrides==='function'){const loaded=ROOT.loadDungeonItemOverrides();overrides=loaded&&typeof loaded==='object'?loaded:{};const prev=overrides[item.id]&&typeof overrides[item.id]==='object'?overrides[item.id]:{};nextOverride={...prev,rpgBonuses:{...(prev.rpgBonuses||{}),[key]:value}};overrides[item.id]=nextOverride}}catch(e){}const writer=findExistingWriter();if(writer){try{if(writer.kind==='bulk')writer.fn.call(ROOT,overrides||{[item.id]:nextOverride||{rpgBonuses:{[key]:value}}});else writer.fn.call(ROOT,item.id,nextOverride||{rpgBonuses:{...(item.rpgBonuses||{})}});persisted=true}catch(e){console.warn('Dungeon equipment UI: override save failed',e)}}try{ROOT.refreshCustomEquipmentIntoItems?.()}catch(e){}scheduleRefresh();return {ok:true,persisted}}
function host(){if(!DOC)return null;return DOC.getElementById('dungeonEquipmentSlots')||DOC.querySelector?.('[data-dungeon-equipment-sheet]')||DOC.querySelector?.('.dungeonEquipmentSlots')||null}
function ensureBox(parent,id){if(!DOC||!parent)return null;let box=DOC.getElementById(id);if(box&&box.parentNode!==parent){try{box.remove()}catch(e){}box=null}if(!box){box=DOC.createElement('div');box.id=id;box.className='deuiFinal';parent.appendChild(box)}return box}
function ensureStyles(){if(!DOC||DOC.getElementById('deuiFinalStyles'))return;const style=DOC.createElement('style');style.id='deuiFinalStyles';style.textContent='.deuiFinal{margin-top:10px}.deuiBlock{margin-top:10px;padding:10px;border:1px solid #444;border-radius:12px;background:#141414}.deuiTitle,.deuiSetName,.deuiItemName{font-weight:900}.deuiSet{margin-top:9px;padding-top:9px;border-top:1px solid #333}.deuiPieces,.deuiHint{font-size:12px;color:#aaa;margin-top:4px}.deuiThresholdGroup{margin-top:8px}.deuiThreshold{font-size:12px;margin-top:4px}.deuiThreshold.next{opacity:.72}.deuiSettings summary{cursor:pointer;font-weight:900}.deuiItemSettings{margin-top:10px;padding-top:9px;border-top:1px solid #333}.deuiGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px;margin-top:7px}.deuiGrid label{display:grid;grid-template-columns:1fr 72px;gap:6px;align-items:center;font-size:12px}.deuiGrid input{width:100%;box-sizing:border-box;background:#0d0d0d;color:#fff;border:1px solid #555;border-radius:7px;padding:6px}@media(max-width:620px){.deuiGrid{grid-template-columns:1fr}}';(DOC.head||DOC.documentElement||DOC.body)?.appendChild(style)}
function refresh(){if(refreshing)return false;const parent=host();if(!parent)return false;refreshing=true;try{ensureStyles();const box=ensureBox(parent,'dungeonEquipmentUiFinal');if(!box)return false;const html=renderSetProgressHtml()+renderItemSettingsHtml();if(box.innerHTML!==html)box.innerHTML=html;return true}finally{refreshing=false}}
function scheduleRefresh(){if(scheduled)return;scheduled=true;const run=()=>{scheduled=false;try{hookRenderer()}catch(e){}try{refresh()}catch(e){console.warn('Dungeon equipment UI refresh',e)}};if(typeof ROOT.requestAnimationFrame==='function')ROOT.requestAnimationFrame(run);else if(typeof setTimeout==='function')setTimeout(run,0);else run()}
function hookRenderer(){const current=ROOT.renderDungeonGear;if(typeof current!=='function'||current.__deuiFinal||current===hookedRenderer)return false;const wrapped=function(){const result=current.apply(this,arguments);scheduleRefresh();return result};wrapped.__deuiFinal=true;wrapped.__deuiOriginal=current;hookedRenderer=wrapped;ROOT.renderDungeonGear=wrapped;return true}
function bind(){if(!DOC||DOC.__deuiFinalBound)return;DOC.__deuiFinalBound=true;DOC.addEventListener?.('change',event=>{const input=event?.target,itemId=input?.dataset?.deuiItem,stat=input?.dataset?.deuiStat;if(itemId&&stat)setEquipmentBonus(itemId,stat,input.value)})}
function observe(){if(!DOC||typeof ROOT.MutationObserver!=='function'||observer)return;observer=new ROOT.MutationObserver(()=>scheduleRefresh());try{observer.observe(DOC.documentElement||DOC.body,{childList:true,subtree:true})}catch(e){}}
function install(){bind();ensureStyles();hookRenderer();refresh();observe();if(DOC&&DOC.readyState==='loading')DOC.addEventListener?.('DOMContentLoaded',scheduleRefresh,{once:true});if(typeof setTimeout==='function')setTimeout(scheduleRefresh,0);return true}
const api={VERSION,BASE_FIELDS,fields,thresholds,getSetProgress,renderSetProgressHtml,settingsForItem,renderItemSettingsHtml,setEquipmentBonus,refresh,install};
Object.defineProperty(api,'supportedBonusFields',{enumerable:true,get:fields});
ROOT.DungeonEquipmentUI=api;install();
})();