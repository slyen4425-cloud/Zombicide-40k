/* GenSrpG Dungeon — authored zone links 16.78.47.
   Isolated to the dedicated authored Dungeon runtime.
   - Main door graph connections remain reversible.
   - Cache / passage destinations are secondary room-library branches, not main graph nodes.
   - Per-hero return stacks restore the exact source cache cell.
   Does not alter Random Dungeon, Capture or Survival runtimes. */
(function(){
"use strict";
const ROOT=typeof window!=="undefined"?window:globalThis;
const DOC=typeof document!=="undefined"?document:null;
const RT_KEY="gensrpg_dungeon_runtime_v2";
const VERSION="1.1.0",APP_VERSION="16.78.47";
let retries=0;
function clone(v){try{return v==null?v:JSON.parse(JSON.stringify(v))}catch(e){return v}}
function readRt(){try{const x=JSON.parse(localStorage.getItem(RT_KEY)||"null");return x&&typeof x==="object"?x:null}catch(e){return null}}
function writeRt(x){try{localStorage.setItem(RT_KEY,JSON.stringify(x||{}));return true}catch(e){return false}}
function authored(){return ROOT.DungeonAuthoredRuntime167839||null}
function builder(){return ROOT.DungeonWorldBuilder167821||null}
function roomApi(){return ROOT.DungeonRoomCreator100||null}
function roomV2(){return ROOT.DungeonRoomCreatorV2||null}
function activeHero(x){const a=Array.isArray(x?.participants)?x.participants:[],i=Math.max(0,Math.min(Math.max(0,a.length-1),Number(x?.index)||0));return String(a[i]||"")}
function authoredContext(){const A=authored(),x=readRt();if(!A?.active?.()||!x?.last?.authoredRuntime167839)return null;const graph=A.graph?.();if(!graph||String(x.last.worldDungeonId||"")!==String(graph.id))return null;const hero=activeHero(x);if(!hero)return null;const s=x.authored167839||{},currentNodeId=String(s?.roomNodes?.[String(Number(x.room)||0)]||s?.heroNodes?.[hero]||x.last.worldNodeId||"");return {A,x,graph,hero,currentNodeId,pos:Number(x?.positions?.[hero])}}
function nodeInfo(graph,nodeId){const node=(graph?.nodes||[]).find(v=>String(v.id)===String(nodeId||""));return node?{node,room:roomApi()?.findRoom?.(node.roomId)||null}:null}
function nodeLabel(graph,nodeId){const p=nodeInfo(graph,nodeId);return String(p?.node?.label||p?.room?.name||"Zone")}
function roomLabel(roomId){const r=roomApi()?.findRoom?.(roomId);return String(r?.name||roomId||"Lieu")}
function entryIndexForRoom(roomId){const room=roomApi()?.findRoom?.(roomId),cells=Array.isArray(room?.cells)?room.cells:[];const i=cells.findIndex(c=>c?.object==="entry");return i>=0?i:0}
function cacheCells(room){const out=[];(room?.cells||[]).forEach((c,i)=>{if(c?.object==="cache")out.push(i)});return out}
function reverseEdge(graph,nodeId,pos){return (graph?.edges||[]).find(e=>String(e?.toNodeId)===String(nodeId||"")&&Number(e?.toEntryIndex)===Number(pos))||null}
function bindingRoomId(graph,b){if(b?.targetRoomId)return String(b.targetRoomId);const n=(graph?.nodes||[]).find(v=>String(v.id)===String(b?.targetNodeId||""));return String(n?.roomId||"")}
function cacheBinding(graph,nodeId,pos){return (graph?.cacheBindings||[]).find(b=>String(b?.sourceNodeId)===String(nodeId||"")&&Number(b?.sourceIndex)===Number(pos)&&bindingRoomId(graph,b))||null}
function branchNodeId(sourceNodeId,sourceIndex,targetRoomId){return "branch_"+String(sourceNodeId).replace(/[^a-zA-Z0-9_-]/g,"_")+"_"+Math.max(0,Math.trunc(Number(sourceIndex)||0))+"_"+String(targetRoomId).replace(/[^a-zA-Z0-9_-]/g,"_")}
function graphWithBranch(graph,b){const roomId=bindingRoomId(graph,b);if(!roomId||!roomApi()?.findRoom?.(roomId))return null;const id=branchNodeId(b.sourceNodeId,b.sourceIndex,roomId),g=clone(graph);g.nodes=Array.isArray(g.nodes)?g.nodes:[];if(!g.nodes.some(n=>String(n.id)===id))g.nodes.push({id,roomId,label:roomLabel(roomId),secondaryBranch167847:true});return {graph:g,nodeId:id,roomId}}
function stacks(x){x.authored167847ReturnStacks=x.authored167847ReturnStacks&&typeof x.authored167847ReturnStacks==="object"?x.authored167847ReturnStacks:{};return x.authored167847ReturnStacks}
function heroStack(x,hero){const s=stacks(x);s[hero]=Array.isArray(s[hero])?s[hero]:[];return s[hero]}
function topReturn(ctx){const s=heroStack(ctx.x,ctx.hero),top=s[s.length-1];if(!top||String(top.targetNodeId)!==String(ctx.currentNodeId))return null;const cell=String(ctx.x?.last?.map?.cells?.[ctx.pos]||"");return cell==="entry"?top:null}
function blocked(){try{return !!ROOT.dungeonRoomExitLocked102?.()}catch(e){return false}}
function performTransition(ctx,graph,targetNodeId,arrivalIndex,stackMode,stackData){if(!ctx?.A?.enterNode||!targetNodeId)return false;if(stackMode!=="return"&&blocked()){try{ROOT.showToast?.("🔒 Cette zone est verrouillée.")}catch(e){}return false}const s=heroStack(ctx.x,ctx.hero);if(stackMode==="push")s.push(stackData);if(stackMode==="return")s.pop();writeRt(ctx.x);const ok=!!ctx.A.enterNode(graph,targetNodeId,{toEntryIndex:Math.max(0,Math.trunc(Number(arrivalIndex)||0))});if(!ok){const fresh=readRt()||ctx.x,fs=heroStack(fresh,ctx.hero);if(stackMode==="push")fs.pop();if(stackMode==="return"&&stackData)fs.push(stackData);writeRt(fresh);return false}try{ROOT.DungeonCore01?.render?.()}catch(e){}try{ctx.A.syncActionButton?.()}catch(e){}return true}
function travelReverse(){const ctx=authoredContext();if(!ctx)return false;const e=reverseEdge(ctx.graph,ctx.currentNodeId,ctx.pos);if(!e)return false;const ok=performTransition(ctx,ctx.graph,e.fromNodeId,e.fromExitIndex,"none");if(ok)try{ROOT.showToast?.("↩ "+nodeLabel(ctx.graph,e.fromNodeId))}catch(e2){}return ok}
function travelCache(){const ctx=authoredContext();if(!ctx)return false;const b=cacheBinding(ctx.graph,ctx.currentNodeId,ctx.pos),branch=b?graphWithBranch(ctx.graph,b):null;if(!b||!branch)return false;const data={sourceNodeId:String(ctx.currentNodeId),sourceIndex:Number(b.sourceIndex),targetNodeId:branch.nodeId,targetRoomId:branch.roomId,at:Date.now()};const ok=performTransition(ctx,branch.graph,branch.nodeId,entryIndexForRoom(branch.roomId),"push",data);if(ok)try{ROOT.showToast?.("🕳️ "+roomLabel(branch.roomId))}catch(e){}return ok}
function travelReturn(){const ctx=authoredContext();if(!ctx)return false;const top=topReturn(ctx);if(!top)return false;const ok=performTransition(ctx,ctx.graph,top.sourceNodeId,top.sourceIndex,"return",top);if(ok)try{ROOT.showToast?.("↩ Retour vers "+nodeLabel(ctx.graph,top.sourceNodeId))}catch(e){}return ok}
function ensureButton(id,label,handler,anchor){if(!DOC||!anchor)return null;let b=DOC.getElementById(id);if(!b){b=DOC.createElement("button");b.id=id;b.type="button";if(anchor.className)b.className=anchor.className;b.onclick=handler;anchor.parentNode?.insertBefore(b,anchor)}b.textContent=label;b.disabled=false;return b}
function removeButton(id){try{DOC?.getElementById(id)?.remove()}catch(e){}}
function paintTravelButtons(){if(!DOC)return false;const anchor=DOC.getElementById("dc01Explore");if(!anchor){removeButton("dwr167846Return");removeButton("dwr167846Cache");return false}const ctx=authoredContext();if(!ctx){removeButton("dwr167846Return");removeButton("dwr167846Cache");return false}const top=topReturn(ctx),rev=!top?reverseEdge(ctx.graph,ctx.currentNodeId,ctx.pos):null,b=cacheBinding(ctx.graph,ctx.currentNodeId,ctx.pos),rid=b?bindingRoomId(ctx.graph,b):"";if(top)ensureButton("dwr167846Return","↩ Retour vers "+nodeLabel(ctx.graph,top.sourceNodeId),travelReturn,anchor);else if(rev)ensureButton("dwr167846Return","↩ Retour vers "+nodeLabel(ctx.graph,rev.fromNodeId),travelReverse,anchor);else removeButton("dwr167846Return");if(b&&rid)ensureButton("dwr167846Cache","🕳️ Entrer dans "+roomLabel(rid),travelCache,anchor);else removeButton("dwr167846Cache");return true}
function hideRoomCacheDestinationEditor(){if(!DOC)return false;const box=DOC.getElementById("drc200Caches");if(!box)return false;const sec=box.closest?.(".drc200Sub");if(!sec||sec.dataset.zoneLinks167846)return false;sec.dataset.zoneLinks167846="1";sec.innerHTML='<div class="drc200Title">🕳️ Cache / passage secret</div><div class="drc200Hint">Place ici uniquement le point physique. Sa destination (grenier, maison, cave, lieu d’intérêt…) se choisit dans le Créateur de donjon et reste indépendante du réseau principal.</div>';return true}
function legacySuggestedRoomId(sourceRoomId,sourceIndex){try{return String(roomV2()?.roomMeta?.(sourceRoomId)?.cacheLinks?.find(l=>Number(l?.sourceIndex)===Number(sourceIndex))?.targetRoomId||"")}catch(e){return ""}}
function saveCacheBinding(graphId,sourceNodeId,sourceIndex,targetRoomId){const B=builder(),g=B?.findDungeon?.(graphId);if(!g)return false;const target=String(targetRoomId||"");if(target&&!roomApi()?.findRoom?.(target))return false;g.cacheBindings=(g.cacheBindings||[]).filter(b=>!(String(b?.sourceNodeId)===String(sourceNodeId)&&Number(b?.sourceIndex)===Number(sourceIndex)));if(target)g.cacheBindings.push({id:"cachebind_"+Date.now().toString(36)+"_"+Math.random().toString(36).slice(2,7),sourceNodeId:String(sourceNodeId),sourceIndex:Number(sourceIndex),targetNodeId:"",targetRoomId:target,kind:"secondary",returnMode:"source"});B.upsertDungeon?.(g);return true}
function escHtml(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
function activeGraphId(){if(!DOC)return "";const name=String(DOC.getElementById?.("drc300Name")?.value||"").trim(),list=builder()?.loadLibrary?.()||[];const exact=list.find(g=>String(g?.name||"")===name);if(exact)return String(exact.id);const active=DOC.querySelector?.("#drc300Library .drc300Card.active [data-open]");return String(active?.dataset?.open||"")}
function renderWorldCacheBindings(){if(!DOC)return false;const box=DOC.getElementById("drc300Caches"),B=builder();if(!box||!B)return false;const graphId=activeGraphId(),g=B.findDungeon?.(graphId);if(!g)return false;const rows=[];for(const node of g.nodes||[]){const room=roomApi()?.findRoom?.(node.roomId);for(const index of cacheCells(room))rows.push({node,room,index})}if(!rows.length){box.innerHTML='<div style="color:#999;font-size:12px">Aucune cache / passage secret placé dans les pièces du réseau principal.</div>';return true}const library=roomApi()?.loadLibrary?.()||[];box.innerHTML=rows.map((r,i)=>{const binding=(g.cacheBindings||[]).find(b=>String(b.sourceNodeId)===String(r.node.id)&&Number(b.sourceIndex)===Number(r.index)),legacy=legacySuggestedRoomId(r.room.id,r.index),selected=String(bindingRoomId(g,binding)||legacy||"");const opts=['<option value="">— aucune destination —</option>'].concat(library.filter(room=>String(room.id)!==String(r.room.id)).map(room=>'<option value="'+escHtml(room.id)+'" '+(selected===String(room.id)?'selected':'')+'>'+escHtml(room.name||room.id)+'</option>')).join("");return '<div class="drc300Card"><b>🕳️ '+escHtml(nodeLabel(g,r.node.id))+' · case '+(r.index+1)+'</b><small>Branche secondaire indépendante du pool principal. Le retour ramène automatiquement ici.'+(legacy&&!binding?' Ancienne destination proposée.':'')+'</small><div class="drc300Actions"><select data-zl-cache="'+i+'" style="flex:1">'+opts+'</select><button type="button" data-zl-save="'+i+'">💾 Lier</button></div></div>'}).join("");box.querySelectorAll("[data-zl-save]").forEach(btn=>btn.onclick=()=>{const i=Number(btn.dataset.zlSave),r=rows[i],sel=box.querySelector('[data-zl-cache="'+i+'"]');if(saveCacheBinding(g.id,r.node.id,r.index,sel?.value||"")){try{ROOT.showToast?.("✅ Branche secondaire enregistrée")}catch(e){}renderWorldCacheBindings()}});return true}
function refreshEditors(){hideRoomCacheDestinationEditor();renderWorldCacheBindings()}
function installCoreRender(){const core=ROOT.DungeonCore01;if(!core||typeof core.render!=="function")return false;if(core.render.__zoneLinks167846)return true;const old=core.render;const wrapped=function(){const out=old.apply(this,arguments);try{paintTravelButtons()}catch(e){}return out};wrapped.__zoneLinks167846=true;wrapped.__zoneLinksOriginal=old;core.render=wrapped;return true}
function install(){const ok=!!builder()&&!!authored();installCoreRender();refreshEditors();paintTravelButtons();if(DOC&&!DOC.__zoneLinks167846Click){DOC.__zoneLinks167846Click=true;DOC.addEventListener("click",e=>{if(e?.target?.closest?.("#drc100Modal,#drc300Modal"))setTimeout(refreshEditors,0)},true)}if(!ok&&retries++<30&&typeof setTimeout==="function")setTimeout(install,100);return ok}
ROOT.DungeonZoneLinks167846={VERSION,APP_VERSION,authoredContext,nodeInfo,reverseEdge,bindingRoomId,cacheBinding,branchNodeId,graphWithBranch,saveCacheBinding,travelReverse,travelCache,travelReturn,paintTravelButtons,hideRoomCacheDestinationEditor,renderWorldCacheBindings,refreshEditors,install};
if(DOC){if(DOC.readyState==="loading")DOC.addEventListener("DOMContentLoaded",install,{once:true});else install()}else install();
})();