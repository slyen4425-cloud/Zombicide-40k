--- a/index.html
+++ b/index.html
@@ -16014,6 +16014,25 @@
   activeModule:gensShellActiveModuleV1,
   returnToPrimaryView:gensReturnToPrimaryViewV1
 });
+const gensShellModuleLaunchProvidersV1=Object.create(null);
+function gensRegisterModuleLaunchV1(moduleId,handler){
+  const id=String(moduleId||"").toLowerCase();
+  if(!["survival","dungeon","capture","pvp"].includes(id)||typeof handler!=="function")return false;
+  gensShellModuleLaunchProvidersV1[id]=handler;
+  return true;
+}
+async function gensStartModuleSessionV1(moduleId=gensShellActiveModuleV1()){
+  const id=String(moduleId||"").toLowerCase();
+  const handler=gensShellModuleLaunchProvidersV1[id];
+  if(typeof handler!=="function")return false;
+  try{return (await handler())===true}
+  catch(e){console.error("GenSrpG Shell startModuleSession",id,e);return false}
+}
+window.GensShellModuleLaunchV1=Object.freeze({
+  register:gensRegisterModuleLaunchV1,
+  activeModule:gensShellActiveModuleV1,
+  startModuleSession:gensStartModuleSessionV1
+});
 function goMenu(){
   const moduleId=gensShellActiveModuleV1();
   const handled=window.GensShellScreenReturnV1.returnToPrimaryView(moduleId);
